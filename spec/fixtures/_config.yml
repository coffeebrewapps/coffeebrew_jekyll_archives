timezone: UTC
url: "https://coffeebrew-jekyll-archives.com"

defaults:
  - scope:
      type: "archives"
    values:
      layout: "archive"
      permalink: /:path/:basename:output_ext
  - scope:
      type: "posts"
    values:
      layout: "default"
